# Action Repo

This repository acts as the source for webhook events to be sent to the Webhook UI Receiver.

## Testing Webhooks
1. Push any commit to `main` to trigger the "Push" event.
2. Create a pull request from a branch to `main` to trigger the "Pull Request" event.
3. Merge a pull request to trigger the "Merge" event (Brownie points functionality).
